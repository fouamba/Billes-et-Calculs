// Service métier : règles pédagogiques (progression, transitions, etc.)
export class PedagogicRulesService {
  // ... méthodes pour appliquer les règles pédagogiques (composition, décomposition, etc.) ...
}
